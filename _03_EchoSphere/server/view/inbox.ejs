<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Gmail Inbox</title>
    <link rel="stylesheet" href="/styles.css" />
    <style>
      body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
        background-color: #f6f8fc;
      }
      .header {
        background-color: #ffffff;
        padding: 16px;
        display: flex;
        align-items: center;
        box-shadow: 0 1px 2px rgba(0, 0, 0, 0.1);
      }
      .header h1 {
        margin: 0;
        color: #202124;
        font-size: 24px;
      }
      .email-list {
        padding: 20px;
      }
      .email-item {
        background-color: white;
        margin-bottom: 10px;
        padding: 15px;
        border-radius: 8px;
        box-shadow: 0 1px 2px rgba(0, 0, 0, 0.05);
        display: flex;
        justify-content: space-between;
        align-items: center;
      }
      .email-item .info {
        display: flex;
        flex-direction: column;
      }
      .email-item .info span.subject {
        font-weight: bold;
        color: #202124;
      }
      .email-item .info span.sender {
        color: #5f6368;
        font-size: 14px;
      }
      .email-item .time {
        color: #5f6368;
        font-size: 12px;
      }
    </style>
  </head>
  <body>
    <div class="header">
      <h1>Inbox</h1>
    </div>

    <div class="email-list">
      <% if (emails.length > 0) { %> <% emails.forEach(email => { %>
      <div class="email-item">
        <div class="info">
          <span class="subject"><%= email.subject %></span>
          <span class="sender">From: <%= email.sender %></span>
        </div>
        <div class="time"><%= email.time %></div>
      </div>
      <% }); %> <% } else { %>
      <p>No emails found.</p>
      <% } %>
    </div>
  </body>
</html>
